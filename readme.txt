We analyze US gun murder data collected by FBI

download-data.R - dowloads csv file data directory

wrangle-data.R - creates a derived datased and saves as R object in rda directory

analysis.R generates a plot and saves it to the figs directory